node_modules
dist
dist-ssr
.DS_Store
*.local
.vite

# tsc -b build artifacts
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# runtime authoring queue
server/queue/

# runtime doubt-chat threads (session ids + transcript mirrors)
server/chat/

# Playwright MCP screenshot/output artifacts
.playwright-mcp/

# graphify: derived code graph (regenerate deterministically with `npm run graph`)
graphify-out/
.cline/
